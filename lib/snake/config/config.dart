import 'package:flutter/material.dart';

abstract class Config {
  static late double size;

  // background grids count
  static const gridCount = 16;

  // snake's initial length in grid count
  static const initialLength = 3;

  static const snakeWidth = 16.0;

  static const appleRadius = 12.0;

  static const lightGreen = Color(0xffaad752);
  static const deepGreen = Color(0xffa2d149);
  static const background = Color(0xff578a34);

// 4a752c
}
